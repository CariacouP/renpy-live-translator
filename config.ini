[Translation]
# Target language code (e.g., en, fr, es, de, it, ja, ru, pt, zh)
TARGET_LANG = en
ENGINE = google

[AI]
# Default AI model for local translation via Ollama
MODEL = qwen3:latest

[Server]
# Local HTTP server port
PORT = 5005
